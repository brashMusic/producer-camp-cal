# Producer Camp — Calendar image

`calendar.png` is auto-rendered from the Producer Camp Google Calendar feed and
embedded in Notion via its raw URL. Refreshed on a schedule by `sync.sh`.
The secret iCal URL lives outside this repo (`~/.producer_camp_ics`) and is never committed.
